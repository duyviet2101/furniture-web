include product-item.pug

mixin product-grid(items)
  //- .product-container
  .container.product-flex
    each item in items
      +product-item(item)
    else 
      p.text-center Không có sản phẩm nào